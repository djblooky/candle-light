using UnityEngine;

public class HoverIcon : MonoBehaviour
{
    // Update is called once per frame
    private void Update()
    {
        RotateTowardsPlayer();
    }

    private void RotateTowardsPlayer()
    {

    }
}
